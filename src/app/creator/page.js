import WorldCreator from "@/components/WorldCreator";

// This page contains the full World Creator tool.
// We keep the existing creator untouched and simply place it inside its own route.
export default function CreatorPage() {
  return <WorldCreator />;
}